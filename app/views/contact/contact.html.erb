<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Contact - The Gossip Project</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
            --hover-gradient: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            min-height: 100vh;
        }

        .contact-section {
            padding: 80px 0;
        }

        .section-title {
            font-weight: 700;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            letter-spacing: -0.5px;
        }

        .contact-card {
            border: none;
            border-radius: 20px;
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(10px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .contact-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(99, 102, 241, 0.15);
        }

        .form-control {
            border-radius: 12px;
            padding: 15px 20px;
            border: 2px solid #e9ecef;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: #6366f1;
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
        }

        .submit-btn {
            background: var(--primary-gradient);
            color: white !important;
            border: none;
            padding: 15px 40px;
            border-radius: 30px;
            font-weight: 500;
            transition: all 0.3s ease;
            width: 100%;
        }

        .submit-btn:hover {
            background: var(--hover-gradient);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(99, 102, 241, 0.3);
        }

        .contact-method {
            padding: 30px;
            border-radius: 15px;
            background: white;
            transition: all 0.3s ease;
        }

        .contact-method:hover {
            transform: translateY(-5px);
        }

        .contact-icon {
            width: 60px;
            height: 60px;
            background: var(--primary-gradient);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
            margin-bottom: 20px;
        }

        .map-container {
            border-radius: 20px;
            overflow: hidden;
            background: var(--primary-gradient);
            min-height: 300px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
        }
    </style>
</head>
<body>
    <section class="contact-section">
        <div class="container">
            <h1 class="display-4 fw-bold text-center section-title mb-5">Contactez-nous</h1>
            
            <div class="row g-5">
                <!-- Formulaire -->
                <div class="col-lg-7">
                    <div class="contact-card p-5">
                        <form>
                            <div class="row g-4">
                                <div class="col-md-6">
                                    <input type="text" class="form-control" placeholder="Votre nom" required>
                                </div>
                                <div class="col-md-6">
                                    <input type="email" class="form-control" placeholder="Votre email" required>
                                </div>
                                <div class="col-12">
                                    <input type="text" class="form-control" placeholder="Sujet">
                                </div>
                                <div class="col-12">
                                    <textarea class="form-control" rows="6" placeholder="Votre message" required></textarea>
                                </div>
                                <div class="col-12">
                                    <button type="submit" class="submit-btn">
                                        <i class="bi bi-send me-2"></i>Envoyer un message
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- Informations de contact -->
                <div class="col-lg-5">
                    <div class="d-flex flex-column gap-4">
                        <div class="contact-method">
                            <div class="contact-icon">
                                <i class="bi bi-envelope"></i>
                            </div>
                            <h4 class="fw-bold mb-2">Email</h4>
                            <a href="mailto:contact@gossipproject.com" class="text-decoration-none">contact@gossipproject.com</a>
                        </div>

                        <div class="contact-method">
                            <div class="contact-icon">
                                <i class="bi bi-telephone"></i>
                            </div>
                            <h4 class="fw-bold mb-2">Téléphone</h4>
                            <a href="tel:+33123456789" class="text-decoration-none">+33 1 23 45 67 89</a>
                        </div>

                        <div class="contact-method">
                            <div class="contact-icon">
                                <i class="bi bi-geo-alt"></i>
                            </div>
                            <h4 class="fw-bold mb-2">Adresse</h4>
                            <p class="mb-0">12 Rue des Potins<br>75000 Paris, France</p>
                        </div>
                    </div>

                    <div class="map-container mt-4">
                        <i class="bi bi-map me-2"></i> Carte (à intégrer)
                    </div>
                </div>
            </div>
</body>            
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

                
